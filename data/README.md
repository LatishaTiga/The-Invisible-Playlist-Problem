Note: `downsampled_1M_tracks.csv` and `audio_features.csv` is not included due to GitHub size limits.

The audio_features.csv file can be regenerated using the sampling logic documented in
`pipeline.py`.
